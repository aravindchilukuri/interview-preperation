// given an array of words return a new array coantaining each word capitalized

function capitalizeWords(str) {
    let word = str.split(' ').join(' ',' ').toUpperCase()
    return word
}

console.log(capitalizeWords('I am aravind'))